include app/form-domain
include app/message-error
include app/favicon

mixin app(title)
  .app
    h1.app__title= title
    .app__form-domain
      +form-domain("form-domain")
    .app__message-error
      +message-error("message-error")
    .app__favicon
      +favicon("favicon")
